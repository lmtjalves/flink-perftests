{
  "machines": [
    {
      # Hostname of the machine
      "hostname": "localhost"

      # Services that should be started on the machine
      "services": [
        {
          # Script to start the service
          "start": "flink/..."

          # Script to stop the service
          "stop": "flink/..."

          # Optional logs directory to collect in order to be analysed
          "logs": "flink/..."
        }
      ]
    }
  ]
}
